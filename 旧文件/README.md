# 旧文件归档

此目录保存新包架构不再依赖、但为了可恢复性暂不删除的内容：

- `兼容入口/`：原根目录导入转发模块。
- `历史文档/`：已由根目录 `ARCHITECTURE.md` 和 `README.md` 汇总取代的阶段性设计稿。
- `历史样例/`：早期风险上下文、统一信号和候选钱包样例。
- `历史备份/`：整理前已有的注册表备份。
- `缓存/`：整理时存在的根目录 Python 缓存；运行时可重新生成。

当前应用、测试与脚本均不引用这里的文件。
